# Backend API Configuration
# The base URL for your backend server
VITE_API_BASE_URL=http://localhost:5000

# AWS S3 Configuration - DEPRECATED
# These are now handled server-side for security
# Keep these commented out as they're no longer needed on the client
# VITE_AWS_ACCESS_KEY_ID=your_aws_access_key_here
# VITE_AWS_SECRET_ACCESS_KEY=your_aws_secret_key_here
# VITE_AWS_REGION=your_aws_region_here
# VITE_AWS_S3_BUCKET_NAME=your_s3_bucket_name_here

# Note: AWS credentials should be configured on the server-side only
# Check the server/.env file for AWS configuration

# Development mode
NODE_ENV=development
